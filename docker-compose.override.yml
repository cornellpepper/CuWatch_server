services:
  db:
    volumes:
      - pgdata_spring2026:/var/lib/postgresql/data

  bridge:
    environment:
      # Debug logging is OFF by default; enable by exporting BRIDGE_DEBUG=1
      BRIDGE_DEBUG: ${BRIDGE_DEBUG:-0}

volumes:
  pgdata_spring2026:
